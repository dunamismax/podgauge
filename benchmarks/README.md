# Benchmarks

This directory will own consented corpus manifests and versioned evaluation
code. The current `foundation.ts` command measures only a deterministic package
smoke path; it is not a scoring, accuracy, or performance claim.
