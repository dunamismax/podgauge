# Infrastructure

`compose/`, `caddy/`, and `scripts/` reserve the production infrastructure
boundaries described in the specification. The repository-root `compose.yaml`
is development-only. No production proxy, registry, secrets, or backup target
has been selected.
