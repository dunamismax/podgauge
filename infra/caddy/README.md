# Caddy assets

Production Caddy examples belong here after the owner selects DNS-only or
Cloudflare-proxied mode. No forwarded client-address header may be trusted
before the corresponding origin restrictions and failure tests exist.
